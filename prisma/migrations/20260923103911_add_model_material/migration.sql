-- This migration's effect (creating "ModelMaterial") was already applied by the
-- earlier migration "20260923101121", which was generated independently on a
-- parallel branch against the same schema state. Left as a no-op so a fresh
-- database (and `prisma migrate dev`'s shadow-db replay) can apply the full
-- migration history in order without a duplicate-table error.
SELECT 1;
